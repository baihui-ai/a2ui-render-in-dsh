# a2ui-render-in-dsh 功能示例

[English](DEMO.md) | 中文

每个示例给出：**你对 dsh 说的话 → 模型生成的卡片 → 你在卡片上的操作 → 回到对话的结果**。所有动图/截图均为真实渲染录制（chromium）。

想在本地跑一遍：安装插件后（见 [README 安装](README.zh.md#安装)），把每节的提示词直接发给 dsh 即可——是否渲染卡片由模型自适应判断，下面这些提示词都会稳定触发。

---

## 1. 交互做题（学习/测验场景）

> 💬 「用交互卡片出一道单选题考我：HTTP 301 与 302 的区别」

模型生成选择题卡片 → 点选项 → 点「提交答案」→ 答案以自然语言消息回传（`提交答案：301 永久重定向，302 临时重定向`）→ 模型判分讲解。**提交后整卡锁定**，选项保持高亮不可再改，底部记录提交内容与时间；刷新页面后锁定态和已选项自动恢复。

![做题交互](docs/demo-quiz.gif)

## 2. 表单（下拉单选/多选、输入、勾选）

> 💬 「做一个课程报名表单：姓名输入、城市下拉单选（默认北京）、方向下拉多选（最多 2 个）、同意协议勾选」

`Select` 下拉支持单选（存值）与多选（存数组、可限最大数）、选项描述、选项级禁用（如"广州（暂未开放）"）、`dataModel` 预选中。提交消息是多行自然语言摘要：

```
提交报名
城市：上海
方向：后端开发、数据分析
同意课程协议：是
```

![表单交互](docs/demo-form.gif)

## 3. 商品卡片（一行多列 + 查询按钮）

> 💬 「推荐三款机械键盘，做成三列商品卡片，每张带查看详情按钮」

`Grid` 一次调用排出等宽多列卡片。**无输入组件的卡片上的按钮自动识别为查询按钮**——可反复点击（每次发送 `查看详情：SKU-00x`），不会像表单那样锁定。

![商品卡片](docs/demo-product.gif)

## 4. 数据图表与仪表盘

> 💬 「画一个 2×2 的运营仪表盘：访问量折线、订单量柱状、渠道占比饼图、转化率面积图」

`Chart` 透传标准 ECharts option，`Grid columns: 2` 排出仪表盘。

![仪表盘](docs/demo-dashboard.png)

## 5. 数学函数绘图

> 💬 「画出 y=tan(x)、cot(x)、sec(x)、csc(x) 的图像」

模型只写表达式（`functions: [{"expr": "tan(x)"}]`），插件内置**安全表达式求值器**（白名单数学函数，非 eval）自动采样 ~400 点、在渐近线处断线——模型无需手工枚举数据点（那样必然画错）。

![函数绘图](docs/demo-function-plot.png)

## 6. 流程图 / 思维导图 / 时序图（Mermaid）

> 💬 「画一个用户注册流程图，再画一个前端技能思维导图」

`Mermaid` 组件覆盖 flowchart、mindmap、sequenceDiagram、gantt、pie、stateDiagram 等全部 Mermaid 图型，明暗主题自动切换。

## 7. 数学公式（KaTeX）

> 💬 「展示正态分布的概率密度函数」「演示矩阵乘法（先给出算式）」

`Math` 组件渲染 LaTeX（字体已内联进插件，零外部请求）。矩阵强制走公式渲染——目录规则禁止模型在文本里写 `[[2,1],[0,3]]` 这类原始数组。

明暗主题下的综合效果（公式 + 图表 + 流程图 + 思维导图 + 选择题同卡混排）：

| 明亮 | 暗色 |
|---|---|
| ![明亮主题](docs/demo-viz-light.png) | ![暗色主题](docs/demo-viz-dark.png) |

## 8. 算法动画 · 数组/柱状形态

> 💬 「用动画演示快速排序 [7,2,9,4,1,8,3]」

模型模拟算法执行输出逐帧状态，插件用渐变柱平滑过渡播放：橙色=当前比较/交换、绿色=已就位，配逐帧解说、进度条、图例。**每卡每页签只自动播放一遍**（重挂载不重播），播完按钮变 ↻，点一次重播一遍；支持暂停、单步、重置。

![排序动画](docs/demo-sort.gif)

## 9. 算法动画 · 网格/矩阵形态

> 💬 「用动画演示矩阵乘法 [[2,1],[0,3]] × [[1,4],[5,2]]」

多矩阵并排逐格计算：橙色=参与计算的行/列、绿色=写入的结果格、逐帧算式解说（`C[0][1] = 1×7 + 2×8 = 23`）。同样适用于 DP 填表等 2D 过程。

![矩阵动画](docs/demo-matrix.gif)

## 10. 全屏放大（思维导图/流程图/图表/图片）

悬停出现 ⛶ → 全屏并自动放大适配视口 → 滚轮缩放（0.2×–10×）+ 拖拽平移 → Esc/✕ 退出。图表全屏时按全屏尺寸重新渲染（矢量级清晰）。

![全屏放大](docs/demo-fullscreen.gif)

---

## 一站式体验提示词

依次发给 dsh，即可复现上面全部功能：

1. 用交互卡片出一道单选题考我：HTTP 301 与 302 的区别
2. 做一个课程报名表单：姓名输入、城市下拉单选（默认北京）、方向下拉多选（最多选 2 个）、同意协议勾选
3. 推荐三款机械键盘，做成三列商品卡片，每张带查看详情按钮
4. 画一个 2×2 运营仪表盘：访问量折线、订单量柱状、渠道占比饼图、转化率面积图
5. 画出 y=sin(x) 与 y=cos(x) 的图像对比，并展示欧拉公式
6. 画一个用户注册流程图和一个前端技能思维导图
7. 用动画演示冒泡排序 [5,2,8,1,9]
8. 用动画演示矩阵乘法 [[1,2],[3,4]] × [[5,6],[7,8]]，先给出完整算式
